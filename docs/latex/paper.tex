\documentclass[11pt,a4paper]{article}

% --- Encoding y tipografía ---------------------------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish,es-tabla]{babel}
\usepackage{lmodern}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{enumitem}

% --- Matemáticas -------------------------------------------------------------
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}

% --- Tablas y figuras --------------------------------------------------------
\usepackage{booktabs}
\usepackage{array}
\usepackage{caption}
\usepackage{subcaption}
\captionsetup{font=small,labelfont=bf,margin=1em}

% --- Color, hyperref ---------------------------------------------------------
\usepackage{xcolor}
% Paleta de marca de la universidad
\definecolor{uniyellow}{HTML}{FFD100}      % amarillo institucional puro (UC3M)
\definecolor{paperblue}{HTML}{8A6D00}      % versión oscura legible del amarillo (texto, bordes)
\definecolor{papercyan}{HTML}{C49A00}      % dorado intermedio (links secundarios)
\definecolor{paperred}{HTML}{B83A3A}
\definecolor{papergreen}{HTML}{3E8E41}
\definecolor{paperorange}{HTML}{D96B1C}    % naranja un poco más rojizo para separarlo del amarillo
\definecolor{paperpurple}{HTML}{6F3FAE}
\definecolor{papergrey}{HTML}{3A3A3A}
\definecolor{paperbg}{HTML}{FFF8DC}        % fondo crema suave que acompaña al amarillo

\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=paperblue,
  citecolor=paperblue,
  urlcolor=papercyan,
  pdftitle={Redes neuronales como aproximadores numéricos para pricing de opciones},
  pdfauthor={Ángel Fernández, Jorge Alfageme, Héctor Pérez}
}

% --- TikZ / pgfplots ---------------------------------------------------------
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{
  positioning,
  arrows.meta,
  shapes.geometric,
  shapes.misc,
  calc,
  fit,
  decorations.pathreplacing,
  backgrounds,
  matrix,
  shadings,
  shadows
}

% --- Estilos TikZ reusables --------------------------------------------------
\tikzset{
  pipebox/.style={
    rectangle, rounded corners=4pt, draw=paperblue, very thick,
    fill=paperbg, minimum height=1.1cm, minimum width=2.7cm,
    align=center, text=papergrey, font=\small\bfseries, drop shadow={shadow xshift=0.6pt,shadow yshift=-0.6pt,opacity=0.35}
  },
  pipearrow/.style={
    -{Stealth[length=2.5mm,width=2mm]}, thick, draw=paperblue
  },
  notebox/.style={
    rectangle, rounded corners=2pt, draw=papergrey!40, fill=white,
    font=\scriptsize\itshape, text=papergrey, align=center, inner sep=3pt
  },
  legendbox/.style={
    rectangle, rounded corners=2pt, draw=papergrey!50, fill=white,
    font=\scriptsize, align=left, inner sep=4pt
  },
  layer/.style={
    circle, draw=paperblue, fill=uniyellow!45, minimum size=4mm, inner sep=0pt
  },
  phasebox/.style={
    rectangle, rounded corners=3pt, draw=paperblue, very thick,
    top color=white, bottom color=uniyellow!55,
    minimum height=1.3cm, minimum width=2.55cm,
    align=center, font=\scriptsize\bfseries
  }
}

% --- Macros varios -----------------------------------------------------------
\newcommand{\param}[1]{\texttt{#1}}
\newcommand{\eq}[1]{\ensuremath{#1}}

\title{Redes neuronales como aproximadores numéricos\\ \large para pricing de opciones bajo Black-Scholes y Heston}
\author{Ángel Fernández Sánchez \and Jorge Alfageme Sotillos \and Héctor Pérez Ledesma}
\date{Proyecto de investigación --- Métodos numéricos}

\begin{document}

\maketitle

\begin{abstract}
Este trabajo estudia bajo qué condiciones una red neuronal profunda puede actuar como un \emph{surrogate} preciso, rápido y diferenciable de una función de pricing de opciones, entendida como sustituto numérico de un solver matemático ya conocido. La motivación no es predecir nada sobre el mercado sino abaratar la evaluación de modelos cuya solución analítica no existe o es cara: una vez entrenada offline a partir de datos sintéticos generados por el propio solver, la red puede sustituir al modelo original en tareas que exigen millones de evaluaciones, como la calibración diaria, el cálculo de Greeks o la simulación masiva de escenarios. Tomamos como referencia central el trabajo de Chen, Didisheim y Scheidegger sobre \emph{deep surrogates}~\cite{chen2025} y como complemento metodológico el de Huge y Savine sobre \emph{differential machine learning}~\cite{huge2020}. Nuestro objeto de estudio son las opciones europeas vainilla bajo dos modelos, Black-Scholes como caso de validación con solución cerrada y Heston como caso principal, donde la integración por Fourier semi-cerrada es lo suficientemente costosa como para justificar el surrogate. El trabajo se articula en torno a cinco experimentos comparativos que varían cada uno una sola dimensión del problema: la elección de métrica de evaluación, la activación de la red, la distribución del muestreo, la eficiencia computacional y el uso de información diferencial en la pérdida. Este documento describe la revisión bibliográfica que sustenta el planteamiento, la metodología experimental, el diseño concreto de cada comparación y la estructura del código que la implementa.
\end{abstract}

\vspace{0.5em}
\noindent\textbf{Palabras clave:} \emph{deep surrogates}, pricing de opciones, Heston, Black-Scholes, \emph{differential machine learning}, métodos numéricos.

\tableofcontents
\vspace{1em}

% =============================================================================
\section{Introducción}
% =============================================================================

El objetivo general de este trabajo es estudiar cómo se pueden usar las redes neuronales como herramienta numérica para aproximar modelos de pricing de opciones. Es decir, no nos interesa la red como un sistema que aprende del mercado, sino como un aproximador funcional capaz de imitar a un solver matemático ya conocido. La distinción puede parecer sutil pero cambia completamente lo que se está haciendo: no estamos prediciendo nada, estamos sustituyendo un cálculo costoso por una aproximación rápida y diferenciable.

En finanzas cuantitativas hay muchos modelos de pricing de opciones, desde el clásico Black-Scholes hasta modelos modernos con volatilidad estocástica, saltos o memoria. Todos ellos tienen una estructura común: dado un conjunto de inputs, que pueden ser características del contrato y parámetros del modelo, devuelven un precio o una volatilidad implícita. El problema es que conforme los modelos ganan realismo también se vuelven más caros computacionalmente. Lo que en Black-Scholes es una fórmula cerrada se convierte en Heston en una integral que hay que resolver por Fourier, y en Bates o en \emph{rough volatility} en algo aún más pesado. Cuando estos modelos hay que evaluarlos miles o millones de veces, por ejemplo en calibración diaria, en cálculo de Greeks o en simulación de escenarios, el coste se vuelve un cuello de botella real.

La idea de los \emph{deep surrogates} es resolver ese cuello de botella entrenando una red neuronal a partir de datos sintéticos generados por el propio modelo, de forma que la red aprenda la función de pricing. Si llamamos $f : \mathcal{X} \subset \mathbb{R}^d \to \mathbb{R}$ a la función de pricing del modelo verdadero, lo que hacemos es entrenar una red $\hat{f}_\theta$ con parámetros $\theta$ para que
\begin{equation}
\hat{f}_\theta(x) \approx f(x), \qquad x \in \mathcal{X}.
\end{equation}
Una vez entrenada, evaluarla es órdenes de magnitud más rápido que llamar al solver original. El esquema general se ilustra en la \autoref{fig:pipeline}.

% --- FIGURA 1: pipeline general ---------------------------------------------
\begin{figure}[ht]
\centering
\begin{tikzpicture}[node distance=8mm and 14mm]
  \node[pipebox] (model) {Modelo\\financiero\\\scriptsize\textnormal{(Heston, Bates,\dots)}};
  \node[pipebox, right=of model] (data)  {Datos\\sintéticos\\\scriptsize\textnormal{$(x_i, f(x_i))$}};
  \node[pipebox, right=of data]  (net)   {Red\\neuronal\\\scriptsize\textnormal{$\hat{f}_\theta(x)$}};
  \node[pipebox, right=of net]   (eval)  {Evaluación\\rápida\\\scriptsize\textnormal{calib., Greeks, MC}};

  \draw[pipearrow] (model) -- node[above, font=\scriptsize\itshape] {muestreo} (data);
  \draw[pipearrow] (data)  -- node[above, font=\scriptsize\itshape] {entrenamiento} (net);
  \draw[pipearrow] (net)   -- node[above, font=\scriptsize\itshape] {inferencia} (eval);

  \node[notebox, below=4mm of data, text width=3.4cm]
    {coste alto pero \emph{offline}};
  \node[notebox, below=4mm of eval, text width=3.4cm]
    {coste bajo y \emph{diferenciable}};

  \begin{pgfonlayer}{background}
    \node[rectangle, rounded corners=6pt, fill=paperbg, draw=papergrey!20,
          fit=(model)(data)(net)(eval), inner sep=8pt] {};
  \end{pgfonlayer}
\end{tikzpicture}
\caption{Esquema general de un \emph{deep surrogate}: el modelo financiero genera datos sintéticos sobre los que se entrena una red que después sustituye al solver original en evaluaciones masivas.}
\label{fig:pipeline}
\end{figure}

La red no reemplaza la teoría financiera, simplemente aprende a imitar al solver. El objetivo no es aplicar las técnicas de \emph{deep learning} a datos de mercado ruidosos, sino utilizar estas técnicas para resolver un problema clásico de aproximación numérica con la ventaja añadida de que el resultado es diferenciable por construcción.

En Black-Scholes la fórmula cerrada ya es muy rápida y un surrogate no aporta valor práctico, pero es ideal como caso de validación, porque conocemos la solución exacta y podemos medir errores de precio, Delta y volatilidad implícita con precisión. El objetivo del proyecto es verificar si se pueden utilizar surrogates con modelos más complejos sin perder precisión, y para ello combinamos Black-Scholes como entorno controlado con Heston como caso principal de estudio. La pregunta de investigación que estructura todo el trabajo se formula así:

\begin{quote}\itshape
Bajo qué condiciones una red neuronal profunda puede actuar como un surrogate preciso, rápido y diferenciable para funciones de pricing de opciones.
\end{quote}

Para responderla de forma operativa, descomponemos la pregunta en cinco preguntas más concretas que se traducen en cinco experimentos comparativos. Cada uno varía una sola dimensión del problema mientras mantiene todo lo demás fijo, lo que permite atribuir cualquier diferencia observada a la dimensión que estamos variando y no a otras variables que se hayan colado por accidente.

El resto del documento se organiza como sigue. La \autoref{sec:revision} sintetiza la revisión bibliográfica que ha guiado las decisiones metodológicas, con especial detalle en la referencia central de Chen, Didisheim y Scheidegger. La \autoref{sec:enfoque} explica cómo enfocamos nuestro propio trabajo a partir de esa base. La \autoref{sec:metodologia} fija la metodología experimental: \emph{targets}, normalización, solvers, dominio, muestreo y evaluación por \emph{bins}. La \autoref{sec:experimentos} describe los cinco experimentos y los once surrogates que los sostienen. La \autoref{sec:fases} presenta el plan de ejecución por fases. La \autoref{sec:discusion} discute alcance, limitaciones y trabajo en curso.

% =============================================================================
\section{Revisión bibliográfica}\label{sec:revision}
% =============================================================================

La primera fase de nuestro trabajo ha consistido en una investigación bibliográfica profunda para tener una visión global de los temas relevantes y situar nuestra propuesta dentro del panorama existente. Hay cinco artículos que hemos trabajado a fondo y que forman el núcleo de la revisión: los tres primeros eran una recomendación de los profesores como punto de partida, un cuarto, el de \emph{deep surrogates}, lo encontramos buscando trabajos más recientes en la línea de aproximación numérica de modelos de pricing, y un quinto, el de \emph{differential machine learning} de Huge y Savine, lo hemos incorporado como complemento al anterior porque ofrece una pieza metodológica que conecta directamente con nuestras preocupaciones sobre la calidad de las derivadas aprendidas.

\subsection{Hutchinson, Lo y Poggio (1994): el punto de partida histórico}

El punto de partida histórico es el trabajo de Hutchinson, Lo y Poggio~\cite{hutchinson1994}. Estos autores proponen un método no paramétrico para estimar la fórmula de pricing de un derivado mediante \emph{learning networks}, en una época en la que la literatura financiera estaba dominada por enfoques paramétricos basados en Black-Scholes y argumentos de no arbitraje. La idea central es invertir la lógica habitual: en lugar de partir de un modelo estocástico para el subyacente y derivar analíticamente la fórmula del precio, dejan que la red aprenda directamente la función que mapea variables económicas observables, como el precio del subyacente normalizado por el strike y el tiempo a vencimiento, al precio de la opción, sin imponer supuestos de lognormalidad ni continuidad de trayectoria. Comparan cuatro arquitecturas, redes de funciones de base radial, perceptrones multicapa, \emph{projection pursuit regression} y mínimos cuadrados ordinarios como baseline, y atacan a la vez los problemas de pricing y delta-hedging, lo cual es importante porque la cobertura exige que la red aproxime bien no solo el precio sino también su derivada respecto al subyacente. Validan primero con datos sintéticos generados por Monte Carlo bajo Black-Scholes y después con datos reales de opciones sobre futuros del S\&P 500 entre 1987 y 1991, donde la red llega a superar a Black-Scholes en \emph{tracking error} de la cartera replicante.

Su relevancia histórica reside en que es el primer trabajo que demuestra de forma sistemática que una red neuronal puede aprender una fórmula de pricing y usarse operativamente para cubrir, abriendo la línea de investigación que conecta con el enfoque actual de \emph{deep surrogates}. La diferencia crucial con nuestro proyecto es que aquí la red aprende del mercado, mientras que un surrogate moderno aprende la salida de un pricer ya conocido para acelerarlo: la red pasa de competir con Black-Scholes a aproximarlo de manera eficiente.

\subsection{Becker, Cheridito y Jentzen (2019): deep optimal stopping}

El segundo paper es el de Becker, Cheridito y Jentzen sobre \emph{deep optimal stopping}~\cite{becker2019}. Aquí los autores proponen un método de \emph{deep learning} para resolver problemas de parada óptima en tiempo discreto, es decir, problemas en los que hay que decidir en cada instante si parar o continuar un proceso de Markov para maximizar la esperanza de un pago. La idea central es que cualquier tiempo de parada admisible se puede descomponer en una sucesión de decisiones binarias, una por cada paso temporal, y que cada una de esas decisiones puede aprenderse como una función del estado actual mediante una red neuronal \emph{feedforward}. Sustituyen la indicadora dura por una sigmoide, lo que permite entrenar los parámetros con ascenso de gradiente estocástico sobre trayectorias Monte Carlo, y proceden por inducción hacia atrás desde el último instante hasta el primero. El método se aplica al pricing de opciones bermudas tipo \emph{max-call} sobre cestas de hasta quinientos activos en un Black-Scholes multidimensional, a un \emph{callable multi barrier reverse convertible} y al problema no markoviano de parar óptimamente un movimiento browniano fraccionario, con precisión alta y tiempos cortos en dimensiones donde los métodos tradicionales sufren la maldición de la dimensionalidad.

Para nuestra revisión es importante porque ilustra una línea claramente distinta de la nuestra: ellos aprenden directamente la política de ejercicio para derivados con derecho de parada anticipada, mientras que en nuestro proyecto la red actuará como surrogate de la función de pricing de opciones europeas. Son enfoques complementarios dentro del uso del \emph{deep learning} en finanzas cuantitativas, pero no resuelven el mismo problema, y conviene tener esa distinción clara para evitar comparaciones que no aplican.

\subsection{Ruf y Wang (2020): mapa del campo}

El tercer paper recomendado es la revisión bibliográfica de Ruf y Wang~\cite{ruf2020}, que funciona como mapa general del campo. Los autores compilan y comparan más de ciento cincuenta artículos en una tabla detallada que cataloga cada estudio según seis dimensiones, entre ellas las variables de entrada, las salidas de la red, los modelos \emph{benchmark}, las métricas de rendimiento, el método de partición de los datos y los activos subyacentes. Cubren tanto el enfoque puramente no paramétrico, donde la red aprende directamente el precio a partir de variables como subyacente, strike, vencimiento y volatilidad, como los enfoques híbridos que combinan fórmulas paramétricas tipo Black-Scholes con correcciones aprendidas, además de variantes más recientes orientadas a calibración, resolución de PDEs, aproximación de funciones de valor en problemas de control óptimo y \emph{deep hedging}.

Lo más útil de este paper son las lecciones que extrae del repaso exhaustivo: la importancia de usar inputs estacionarios como la moneyness en lugar de precio y strike por separado, la necesidad de elegir \emph{benchmarks} que no trivialicen la comparación y, sobre todo, el problema del \emph{data leakage} que aparece cuando la partición train/test se hace de forma aleatoria sobre datos con estructura temporal, rompiendo el orden cronológico, contaminando el test e infraestimando sistemáticamente el error de generalización. Para nuestro trabajo es una referencia útil porque ofrece un panorama de la disciplina, permite situar nuestra propuesta dentro de las distintas líneas históricas y nos advierte de errores típicos que conviene evitar, aunque la mayor parte de la bibliografía que analiza versa sobre el uso de técnicas de \emph{machine learning} directamente sobre datos de mercado y no sobre el problema de aproximar un solver conocido.

\subsection{Chen, Didisheim y Scheidegger (2025): referencia central}\label{sec:chen}

El cuarto paper, y el que ha terminado convenciendonos como referencia central, es el de Chen, Didisheim y Scheidegger sobre \emph{deep surrogates} en finanzas~\cite{chen2025}. Este artículo propone los \emph{deep surrogates} como una clase de aproximadores numéricos para modelos estructurales costosos, sustituyendo la función original por una red neuronal profunda entrenada con muestras simuladas que toma exactamente los mismos inputs y devuelve la misma salida a un coste computacional drásticamente menor, además de proporcionar gradientes a un coste que no escala con el número de inputs. El paper aplica esta idea al modelo de Bates con saltos doble exponenciales, que tiene cinco variables de estado y ocho parámetros estructurales, resultando en un input de trece dimensiones, y entrena la red sobre mil millones de observaciones sintéticas para mapear ese input a la volatilidad implícita Black-Scholes asociada.

Una idea crucial del paper es que la maldición de la dimensionalidad queda mitigada por dos factores que se refuerzan entre sí. Por un lado, los datos de entrenamiento se generan de manera sintética y determinista a partir del propio modelo, sin ruido estadístico, lo que elimina la necesidad de cubrir el espacio de inputs con una densidad de muestras que permita promediar errores aleatorios. Por otro lado, la función de pricing en modelos afines como Heston o Bates es analíticamente suave, lo que controla el problema de aproximación y permite a una red profunda representar el mapa de inputs a precio con un número de parámetros que escala de forma manejable con la dimensión. Esta intuición se apoya en resultados teóricos: el trabajo clásico de Barron~\cite{barron1993} sobre aproximación de funciones con norma de Barron acotada y, más recientemente, los de Grohs, Hornung, Jentzen y von Wurstemberger~\cite{grohs2018} demuestran formalmente cómo las redes neuronales profundas escapan a la maldición de la dimensionalidad en la aproximación de soluciones de la ecuación de Black-Scholes en alta dimensión. Juntas, estas dos propiedades justifican que un \emph{deep surrogate} sea una herramienta de aproximación numérica plausible en problemas de dimensión moderada-alta, no solo un truco que funciona empíricamente.

El paper también argumenta de forma explícita por qué las redes neuronales son la familia de aproximadores adecuada frente a alternativas naturales como polinomios globales, \emph{splines}, \emph{sparse grids} o \emph{Gaussian processes}. Los polinomios funcionan razonablemente en funciones suaves pero les cuesta capturar rasgos locales fuertes como \emph{kinks}, y eso en opciones es un problema serio porque el \emph{payoff} tiene un \emph{kink} claro en el strike, con oscilaciones de Runge si se intenta una aproximación global. Los \emph{splines} capturan bien rasgos locales por tramos pero escalan muy mal en alta dimensión. Las \emph{sparse grids} mitigan parte del problema de las mallas cartesianas pero rinden peor en dominios irregulares, frecuentes en finanzas por las restricciones entre parámetros. Los \emph{Gaussian processes} son elegantes pero su coste matricial $O(n^3)$ los hace inviables con millones de observaciones. Las redes neuronales son las únicas que cumplen los cuatro criterios de forma razonable, como resume la \autoref{tab:metodos}.

\begin{table}[ht]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Método & Alta dimensión & No linealidades & Dominio irregular & Escala $n$ \\
\midrule
Polinomios globales       & \textcolor{paperred}{$-$}       & \textcolor{paperred}{$-$}       & \textcolor{paperorange}{$\sim$} & \textcolor{papergreen}{$+$} \\
\emph{Splines}            & \textcolor{paperred}{$-$}       & \textcolor{papergreen}{$+$}    & \textcolor{paperorange}{$\sim$} & \textcolor{paperorange}{$\sim$} \\
\emph{Sparse grids}       & \textcolor{paperorange}{$\sim$} & \textcolor{papergreen}{$+$}    & \textcolor{paperred}{$-$}       & \textcolor{paperorange}{$\sim$} \\
\emph{Gaussian processes} & \textcolor{papergreen}{$+$}    & \textcolor{papergreen}{$+$}    & \textcolor{papergreen}{$+$}    & \textcolor{paperred}{$-$} \\
\textbf{Redes neuronales} & \textcolor{papergreen}{\textbf{+}} & \textcolor{papergreen}{\textbf{+}} & \textcolor{papergreen}{\textbf{+}} & \textcolor{papergreen}{\textbf{+}} \\
\bottomrule
\end{tabular}
\caption{Comparación cualitativa de aproximadores numéricos en los cuatro criterios relevantes para \emph{surrogates} de pricing, en línea con el argumento de Chen et al. Las redes neuronales son las únicas que cumplen los cuatro de forma razonable.}
\label{tab:metodos}
\end{table}

A esta capacidad de aproximación se añade una ventaja decisiva en el contexto de los métodos numéricos: el jacobiano de la red está disponible a un coste muy reducido mediante diferenciación automática. Esto es crítico en finanzas porque muchas magnitudes que importan no son el precio en sí sino sus derivadas, las \emph{Greeks}, que en el modelo original suelen calcularse por diferencias finitas a un coste considerable. Chen et al. documentan que la calibración diaria del modelo de Bates pasa de las aproximadamente $125$ horas que requiere la inversión FFT a poco más de dos horas en CPU y unas decenas de minutos en GPU cuando se usa el surrogate, una reducción de órdenes de magnitud que abre la puerta a aplicaciones empíricas hasta entonces inviables.

Cuatro decisiones técnicas concretas del paper son las que guían directamente las nuestras y se justifican en detalle en la \autoref{sec:metodologia} y la \autoref{sec:experimentos}. Primero, usar una activación suave (Swish) en lugar de ReLU: ReLU produce errores de precio comparables pero Deltas sustancialmente peores porque su derivada es discontinua en cero, mientras que las activaciones suaves dan derivadas estables; esta observación motiva directamente nuestro experimento E2. Segundo, evaluar en volatilidad implícita Black-Scholes en vez de en precio, porque IV es la unidad común que pone todas las opciones en una escala homogénea sin importar moneyness ni vencimiento; nosotros mantenemos el precio como \emph{target} de entrenamiento pero evaluamos en ambas escalas, y esa comparación es justamente E1. Tercero, definir un hipercubo amplio y muestrear puntos aleatorios dentro de él, recortando un cinco por ciento de los bordes en evaluación para no contaminar las métricas con errores de frontera; este esquema es la base de nuestro pipeline de generación de datos y E3 explora muestreos no uniformes dentro de él. Cuarto, prescindir de la regularización clásica del \emph{machine learning} (sin \emph{early stopping}, sin \emph{dropout}, sin \emph{weight decay}) porque los datos son sintéticos y deterministas, sin nada que sobreajustar.

La validación que hace el paper del aproximador es la otra pieza que copiamos. Separan explícitamente dos niveles de error: el error de aproximación, que mide cuánto se aleja el surrogate del solver original para parámetros conocidos y se reporta por \emph{bins} de moneyness y vencimiento, y el error de estimación, que mide el coste económico de calibrar con el surrogate dentro de un procedimiento de optimización. En el primer nivel reportan que el percentil 95 del error absoluto en volatilidad implícita y en Delta queda por debajo de $8\cdot 10^{-4}$ para opciones con más de siete días a vencimiento. Esta distinción entre los dos niveles de error es la que nos lleva a evaluar siempre por \emph{bins} en lugar de reportar promedios globales y a separar precio, IV y Delta como métricas independientes en los cinco experimentos.

\subsection{Huge y Savine (2020): differential machine learning}

El quinto paper es el de Huge y Savine sobre \emph{differential machine learning}~\cite{huge2020}. Originalmente lo teníamos como uno más del panorama, pero al pensar en cómo podríamos innovar respecto al paper de \emph{deep surrogates} nos hemos dado cuenta de que ofrece una pieza metodológica muy potente y muy alineada con nuestras preocupaciones. La idea es que cuando se entrena un surrogate con datos sintéticos, normalmente la red solo ve el valor de la función en cada punto, por ejemplo el precio de la opción, y aprende a predecirlo. Sin embargo, si el modelo verdadero es diferenciable, también se conoce la pendiente de la función en cada punto, es decir, las \emph{Greeks}. \emph{Differential machine learning} aprovecha esa información añadiendo los gradientes verdaderos al conjunto de entrenamiento y modificando la pérdida para que la red no solo estime el precio, sino también su derivada respecto a los inputs. El resultado es que se consiguen aproximaciones mucho más precisas, especialmente en términos de \emph{Greeks}, con datasets sustancialmente más pequeños.

Para nuestro trabajo es una referencia clave por dos razones. La primera es que conecta directamente con la necesidad de validar \emph{Greeks} que ya teníamos heredada del paper de Chen et al. La segunda es que tiene una motivación matemática sólida desde el punto de vista de aproximación de funciones, análoga a la que justifica la interpolación de Hermite frente a la clásica: disponer del valor y de la derivada de una función en cada punto muestreado permite reconstruirla con muchos menos puntos. La \autoref{fig:hermite} ilustra esta intuición.

% --- FIGURA: Hermite vs Lagrange ---------------------------------------------
\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.46\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
  width=\textwidth, height=5.2cm,
  axis lines=middle, axis line style={-{Latex[length=2mm]}, papergrey},
  xmin=-0.2, xmax=3.2, ymin=-1.2, ymax=1.6,
  xtick=\empty, ytick=\empty,
  enlargelimits=false, clip=true,
  every axis/.append style={font=\scriptsize, color=papergrey},
  legend style={draw=none, fill=none, font=\scriptsize, at={(0.02,0.98)}, anchor=north west}
]
  % función verdadera
  \addplot[domain=0:3, samples=120, paperblue, very thick] {sin(deg(2*x))*exp(-0.25*x)};
  \addlegendentry{$f(x)$}
  % interpolación lineal (Lagrange con 2 puntos)
  \addplot[domain=0:3, samples=2, paperred, very thick, dashed] {sin(deg(0))*exp(0) + (sin(deg(6))*exp(-0.75) - sin(deg(0))*exp(0))/3 * x};
  \addlegendentry{aprox. solo con valores}
  % nodos
  \addplot[only marks, mark=*, mark size=1.4pt, paperblue] coordinates {(0,0) (3,-0.218)};
\end{axis}
\end{tikzpicture}
\caption{Aproximación que usa solo valores de la función en dos nodos.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.46\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
  width=\textwidth, height=5.2cm,
  axis lines=middle, axis line style={-{Latex[length=2mm]}, papergrey},
  xmin=-0.2, xmax=3.2, ymin=-1.2, ymax=1.6,
  xtick=\empty, ytick=\empty,
  enlargelimits=false, clip=true,
  every axis/.append style={font=\scriptsize, color=papergrey},
  legend style={draw=none, fill=none, font=\scriptsize, at={(0.02,0.98)}, anchor=north west}
]
  \addplot[domain=0:3, samples=120, paperblue, very thick] {sin(deg(2*x))*exp(-0.25*x)};
  \addlegendentry{$f(x)$}
  % Hermite cúbico exacto con valores f(0)=0, f'(0)=2 y f(3) aprox, f'(3) aprox
  % usamos un cúbico simbólico que casa pendientes
  \addplot[domain=0:3, samples=120, papergreen, very thick, dashed]
    {2*x - 1.6*x*x + 0.31*x*x*x};
  \addlegendentry{aprox. con valor y pendiente}
  \addplot[only marks, mark=*, mark size=1.4pt, paperblue] coordinates {(0,0) (3,-0.218)};
  % tangentes
  \draw[paperorange, thick] (axis cs:0,-0.5) -- (axis cs:0.6,0.7);
  \draw[paperorange, thick] (axis cs:2.5,0.6) -- (axis cs:3.3,-0.9);
\end{axis}
\end{tikzpicture}
\caption{Aproximación que incorpora el valor \emph{y} la derivada en cada nodo (estilo Hermite).}
\end{subfigure}
\caption{Motivación matemática del \emph{differential machine learning}: añadir información sobre la derivada en cada nodo permite reconstruir la función con muchos menos puntos. Los segmentos naranja representan las pendientes conocidas.}
\label{fig:hermite}
\end{figure}

\subsection{Otras líneas complementarias}

Más allá de estos cinco pilares, hemos revisado otros trabajos que ayudan a entender que la aplicación de redes neuronales a derivados ha avanzado en varias direcciones complementarias. Una primera línea ataca de frente el problema de resolver las ecuaciones diferenciales parciales que aparecen en el pricing de derivados de alta dimensión, donde los métodos de mallado tradicionales chocan con la maldición de la dimensionalidad: en esa familia se inscribe el \emph{Deep Galerkin Method} de Sirignano y Spiliopoulos~\cite{sirignano2018}, que entrena una red profunda para satisfacer el operador diferencial sobre puntos muestreados al azar y prescinde por completo de la malla, así como el \emph{Deep BSDE Solver} de Han, Jentzen y E~\cite{han2018}, que reformula la PDE como una ecuación diferencial estocástica retrógrada y aproxima el gradiente de la solución mediante redes neuronales, demostrando viabilidad incluso en problemas de cien dimensiones.

Una segunda línea busca acelerar la valoración aprendiendo directamente la función de precios, y aquí el trabajo de Ferguson y Green~\cite{ferguson2018} muestra que una red profunda entrenada con datos sintéticos generados por Monte Carlo puede valorar opciones \emph{basket} millones de veces más rápido que el modelo subyacente. Una tercera vertiente aborda la cobertura de carteras como un problema de aprendizaje por refuerzo, como en el \emph{deep hedging} de Buehler et al.~\cite{buehler2019}, que parametriza la estrategia de cobertura mediante redes neuronales y la optimiza directamente bajo medidas de riesgo convexas, incorporando de forma natural costes de transacción y otras fricciones de mercado sin necesidad de \emph{Greeks}. La calibración de modelos estocásticos también se ha beneficiado de estas técnicas: trabajos recientes aplican \emph{differential ML} al modelo de Heston para reducir drásticamente el tiempo de calibración~\cite{dlcalib2023}.

Después de revisar todo este material, la conclusión a la que hemos llegado es que el paper de \emph{deep surrogates} es el que mejor encaja con nuestros objetivos. La razón principal es que aborda un problema que en la práctica de la industria es muy importante, abaratar el coste computacional de los modelos de pricing y reducir los tiempos de calibración y estimación de parámetros, y son temas muy actuales en mesas de derivados y en \emph{research} cuantitativo a medida que los modelos se vuelven más realistas y por tanto más caros de evaluar. Además, desde el punto de vista de métodos numéricos, el enfoque de \emph{surrogates} es el más completo e interesante porque entrelaza muestreo, aproximación de funciones, validación numérica y cálculo de derivadas.

% =============================================================================
% (sección "Análisis del paper de referencia" disuelta dentro de §2.4
%  y de las secciones de metodología y diseño experimental)
% =============================================================================

% =============================================================================
\section{Enfoque del trabajo}\label{sec:enfoque}
% =============================================================================

Después de digerir esta bibliografía, la decisión más importante es cómo enfocar nuestro propio trabajo para complementar y aportar valor al material existente dentro de nuestras limitaciones temporales y computacionales. La estructura más coherente es trabajar en dos niveles. El primer nivel es Black-Scholes como caso de validación. Black-Scholes en la práctica no necesita un surrogate porque la fórmula cerrada ya es muy rápida, pero precisamente por eso es ideal como entorno de prueba: conocemos la solución exacta, conocemos las Deltas analíticas y podemos comparar precio, volatilidad implícita y \emph{Greeks} de forma directa antes de complicar el problema. El segundo nivel es extender a Heston. Heston introduce volatilidad estocástica, es bastante más realista y su evaluación requiere métodos numéricos como Fourier semi-cerrado, así que la motivación de tener un surrogate empieza a cobrar sentido real. Es un paso intermedio entre Black-Scholes y Bates que conserva la esencia del paper de referencia sin obligarnos a implementar saltos doble exponenciales.

A partir de ahí, hemos identificado cinco puntos en los que centrar nuestro proyecto de investigación, que enumeramos aquí y desarrollamos en la \autoref{sec:experimentos} como experimentos formales.

El primero es comparar de forma sistemática el error en precio frente al error en volatilidad implícita. Planteamos como pregunta metodológica explícita hasta qué punto puede un surrogate tener un error en precio aparentemente bajo pero un error relevante en \emph{implied volatility} en ciertas zonas del dominio. Esto acerca el trabajo a la práctica real del mercado, donde la volatilidad implícita es la unidad natural de comparación.

El segundo elemento diferencial es comparar funciones de activación con foco en la calidad de los \emph{Greeks}. Entrenaremos redes con la misma arquitectura cambiando solo la activación, probando ReLU, \emph{Softplus}, \emph{Swish/SiLU} y \emph{tanh}, y compararemos el MAE de precio, el MAE de Delta y el MAE de volatilidad implícita en cada caso. La hipótesis, alineada con Chen et al., es que las activaciones suaves producen derivadas más estables aunque los errores de precio sean parecidos. Mostrar que dos redes con MAE de precio similar pueden tener Deltas muy distintas es una conclusión potente para un trabajo de métodos numéricos.

El tercer elemento es comparar muestreo uniforme frente a muestreo enfocado. Generaremos dos \emph{datasets}, uno con muestreo uniforme en todo el dominio y otro con más densidad cerca del \emph{at-the-money} y de vencimientos cortos, donde la función de pricing tiene más curvatura. Evaluaremos ambos surrogates con la misma división por \emph{bins} y analizaremos si el muestreo enfocado reduce el error en regiones críticas. Esto conecta directamente con ideas clásicas de métodos numéricos como la elección adaptativa de puntos de malla.

El cuarto elemento es la medición seria de eficiencia computacional. En Black-Scholes la ganancia será probablemente pequeña porque la fórmula cerrada ya es rapidísima, pero el experimento sirve para validar el pipeline. En Heston mediremos el tiempo necesario para valorar grandes volúmenes de opciones con el solver original frente al surrogate, en función del tamaño del lote, porque la red aprovecha mejor el paralelismo cuando procesa muchos puntos a la vez y reportar un único número aislado sería engañoso.

El quinto elemento, central en nuestro planteamiento, es estudiar el efecto de la pérdida en la calidad del surrogate con especial atención al enfoque de \emph{differential machine learning} de Huge y Savine. En lugar de entrenar la red solo con precios, modificamos la pérdida para que también penalice la diferencia entre la Delta verdadera y la Delta que sale por \emph{autograd} de la red. De este modo, la pérdida queda como una combinación ponderada del error de precio y del error de la derivada y la red aprende simultáneamente a acertar el nivel y la pendiente de la función. El planteamiento tiene una motivación matemática sólida (\autoref{fig:hermite}), análoga a la que justifica la interpolación de Hermite frente a la clásica: disponer del valor y de la derivada en cada punto muestreado permite reconstruir la función con muchos menos puntos. La hipótesis asociada es que el \emph{differential machine learning} ofrece una ventaja sustancial frente al entrenamiento con pérdidas estándar en regímenes de \emph{datasets} pequeños y que esa ventaja se atenúa conforme crece el tamaño de la muestra.

% =============================================================================
\section{Metodología}\label{sec:metodologia}
% =============================================================================

Esta sección concreta cómo se traducen las preguntas anteriores en un pipeline experimental ejecutable. Su función no es añadir más tareas sino fijar cómo se ejecuta cada experimento, qué magnitudes se entrenan, qué métricas se reportan y bajo qué condiciones consideramos que una comparación es limpia. En un proyecto de \emph{surrogates}, la parte delicada no es solo entrenar redes, sino asegurar que el dato sintético, la normalización, la métrica y la evaluación responden exactamente a la pregunta que se quiere contestar.

La idea central del proyecto es combinar dos líneas de la literatura. De Chen, Didisheim y Scheidegger~\cite{chen2025} tomamos la lógica de \emph{deep surrogates}: construir una red que aproxime un solver financiero caro a partir de datos sintéticos generados por el propio modelo. De Huge y Savine~\cite{huge2020} tomamos, de forma acotada, la idea de \emph{differential machine learning}: si además del valor de la función conocemos una derivada económicamente relevante, podemos enseñar a la red la forma local de la función y no solo su nivel. En nuestro caso, esa derivada será la Delta.

\subsection{Pipeline experimental}

Todos los experimentos siguen el mismo flujo base. Primero se define un dominio de inputs y una distribución de muestreo. Después se genera un \emph{dataset} sintético evaluando el solver verdadero en cada punto. A continuación se entrena un surrogate con una configuración cerrada y versionada. Finalmente se evalúa el surrogate sobre un conjunto independiente, separando los resultados por \emph{bins} de moneyness y vencimiento. La separación entre generación, entrenamiento y evaluación es importante porque evita contaminar las conclusiones: el \emph{sampler} decide dónde preguntamos al modelo verdadero, el solver decide cuáles son los \emph{targets}, el \emph{trainer} decide cómo aprende la red y el evaluador decide cómo medimos el error. La \autoref{fig:mlp} esquematiza la arquitectura concreta de la red que usamos y la relación entre el \emph{target} de precio y la Delta obtenida por \emph{autograd}.

% --- FIGURA: MLP y autograd para Delta ---------------------------------------
\begin{figure}[ht]
\centering
\begin{tikzpicture}[x=1.2cm, y=0.9cm, every node/.style={font=\scriptsize}]
  % Inputs
  \node[layer, fill=uniyellow!75, draw=paperblue, label={left:\scriptsize $m$}]  (i1) at (0, 2.4)  {};
  \node[layer, fill=uniyellow!75, draw=paperblue, label={left:\scriptsize $T$}]  (i2) at (0, 1.2)  {};
  \node[layer, fill=uniyellow!75, draw=paperblue, label={left:\scriptsize $r$}]  (i3) at (0, 0)    {};
  \node[layer, fill=uniyellow!75, draw=paperblue, label={left:\scriptsize $\boldsymbol{\Theta}$}] (i4) at (0, -1.2) {};
  % Hidden layers (3 layers shown stylized of 4)
  \foreach \k in {1,2,3,4} {
    \foreach \j in {1,...,5} {
      \pgfmathsetmacro\yy{2.4 - (\j-1)*0.9}
      \node[layer] (h\k\j) at (\k*1.4, \yy) {};
    }
  }
  % output
  \node[layer, fill=paperorange!30, draw=paperorange, label={right:\scriptsize $\hat{y}=C/K$}] (o1) at (6.6, 1.2) {};

  % connections input -> first hidden (sparse)
  \foreach \i in {i1,i2,i3,i4} {
    \foreach \j in {1,...,5} {
      \draw[papergrey!35, line width=0.2pt] (\i) -- (h1\j);
    }
  }
  % between hidden
  \foreach \k [evaluate=\k as \kn using int(\k+1)] in {1,2,3} {
    \foreach \j in {1,...,5} {
      \foreach \m in {1,...,5} {
        \draw[papergrey!25, line width=0.15pt] (h\k\j) -- (h\kn\m);
      }
    }
  }
  % hidden -> output
  \foreach \j in {1,...,5} {
    \draw[papergrey!35, line width=0.2pt] (h4\j) -- (o1);
  }

  % bracket layers
  \draw[decorate, decoration={brace, amplitude=4pt}, paperblue]
    (1.4-0.35, 2.95) -- (4*1.4+0.35, 2.95)
    node[midway, above=4pt, font=\scriptsize\bfseries, paperblue] {4 capas $\times$ 128 unidades (Swish)};

  % autograd arrow
  \draw[-{Stealth[length=2mm]}, paperpurple, very thick]
    (o1.south) .. controls (5.8, -1.4) and (2.5, -2.6) ..
    node[midway, below=4pt, font=\scriptsize\itshape, paperpurple, align=center]
      {$\hat{\Delta} = \partial \hat{y} / \partial m$\\ por \emph{autograd}}
    (i1.south);
\end{tikzpicture}
\caption{Arquitectura del surrogate: un MLP de cuatro capas ocultas y 128 unidades con activación Swish que toma como entrada la moneyness, el vencimiento, el tipo y los parámetros del modelo, y produce el precio normalizado $\hat{y}=C/K$. La Delta del surrogate se obtiene derivando $\hat{y}$ respecto a la moneyness por \emph{autograd}, sin diferencias finitas.}
\label{fig:mlp}
\end{figure}

\subsection{Targets, normalización y métricas}

El \emph{target} principal de entrenamiento es el precio normalizado por strike, $y = C/K$, y el input financiero principal es la moneyness simple, $m = S/K$. Esta decisión elimina una escala redundante del problema: si \emph{spot} y \emph{strike} se multiplican por la misma constante, el precio de una \emph{call} europea escala con el \emph{strike}. Trabajar con $C/K$ y $S/K$ permite que la red aprenda una función más estable y facilita la interpretación de la Delta, porque
\begin{equation}
\Delta = \frac{\partial C}{\partial S} = \frac{\partial (C/K)}{\partial (S/K)} = \frac{\partial y}{\partial m}.
\end{equation}
No usamos la moneyness normalizada de Chen et al. porque nuestro objetivo no es replicar la escala empírica del S\&P 500, sino construir un entorno controlado donde la relación entre precio normalizado y Delta sea directa. También fijamos $q = 0$ en todos los experimentos: el \emph{dividend yield} no forma parte de las preguntas centrales del trabajo y eliminarlo reduce la dimensión del dominio sin afectar a las comparaciones.

Además de esta normalización financiera, todos los inputs se normalizan numéricamente a $[0, 1]$ antes de entrar en la red. Esta normalización no cambia el problema matemático pero mejora la estabilidad del optimizador porque evita que dimensiones como $T$, $\rho$, $v_0$ o $\kappa$ vivan en escalas muy distintas. Cuando se calculan derivadas de la red se aplica la regla de la cadena para volver a la escala financiera. En particular, si $m_{\text{norm}} = (m - m_{\min}) / (m_{\max} - m_{\min})$, entonces
\begin{equation}
\hat{\Delta} = \frac{\partial \hat{y}}{\partial m} = \frac{1}{m_{\max} - m_{\min}} \cdot \frac{\partial \hat{y}}{\partial m_{\text{norm}}}.
\end{equation}
La Delta del surrogate se calcula con \emph{autograd} sobre la red, no con diferencias finitas. La Delta verdadera del \emph{dataset} viene del solver ($N(d_1)$ en Black-Scholes y $P_1$ en Heston), mientras que la Delta predicha se obtiene derivando $\hat{y}$ respecto a $m_{\text{norm}}$ y aplicando la corrección anterior. En evaluación se usa \param{create\_graph=False} porque solo se mide la derivada. En el experimento que entrena con pérdida diferencial se usa \param{create\_graph=True} porque la pérdida contiene $\text{MAE}(\Delta)$ y PyTorch necesita derivar esa pérdida respecto a los pesos de la red. Esto incrementa coste y memoria pero es el coste esperado del entrenamiento diferencial.

La volatilidad implícita Black-Scholes no será el \emph{target} principal de entrenamiento, sino una métrica de evaluación. Esta elección mantiene el entrenamiento más directo, porque el solver genera precios, y evita introducir fallos de inversión de volatilidad implícita durante la generación del \emph{dataset}. Al mismo tiempo, evaluar también en IV permite comprobar si un error pequeño en precio es realmente pequeño en la escala que se usa en mercado.

Las métricas mínimas de evaluación son $\text{MAE}(C/K)$, el error absoluto en precio normalizado, $\text{MAE}_{\text{IV}}$ y $\text{MAE}_{\Delta}$. Para cada una se reportan promedios y percentiles por \emph{bin}, con especial atención al percentil 95, porque los errores de cola son los que revelan si el surrogate falla en regiones operativamente importantes.

\subsection{Solvers y validación numérica}

Black-Scholes funciona como entorno de control. El precio, la Delta, la Vega y la inversión a volatilidad implícita tienen fórmulas cerradas o procedimientos numéricos muy estables, así que cualquier fallo en Black-Scholes apunta a un error del \emph{pipeline} y no a una dificultad intrínseca del modelo.

Heston es el caso principal. El precio se calcula mediante una formulación semi-cerrada de Fourier, expresada en términos de las probabilidades $P_1$ y $P_2$,
\begin{equation}
C = S e^{-qT} P_1 - K e^{-rT} P_2.
\end{equation}
Esta formulación tiene una ventaja metodológica importante para el experimento de DML: la Delta de una \emph{call} europea se obtiene como $\Delta = e^{-qT} P_1$. Si fijamos $q = 0$, entonces $\Delta = P_1$. Esta Delta es el \emph{target} diferencial del experimento E5. Antes de generar \emph{datasets} con Delta, la implementación se valida contra diferencias finitas centrales en una \emph{grid} pequeña de puntos representativos del dominio. La validación no busca que las diferencias finitas sean el método principal, sino detectar errores de implementación, escalado o inestabilidad numérica. El criterio de aceptación es error absoluto medio inferior a $10^{-4}$ en Delta; los errores extremos se revisan manualmente porque pueden señalar problemas de integración, vencimientos demasiado cortos o puntos cercanos a frontera.

Más allá de los tests internos, la implementación se valida también de forma cruzada con QuantLib sobre una \emph{grid} representativa de inputs. Esta validación externa cubre precio y Delta en Black-Scholes y en Heston, y es lo que cierra la primera fase del proyecto antes de generar \emph{datasets} grandes. Mantener esta capa de validación es lo que permite descartar que cualquier patrón posterior se deba a un error silencioso en el solver, escenario que contaminaría todo el resto del trabajo.

La inversión a volatilidad implícita se usa solo en evaluación. El inversor debe controlar precios fuera de límites de arbitraje, zonas de Vega baja y fallos de convergencia. Los fallos no se ocultan: se reportan como parte del diagnóstico, especialmente en vencimientos muy cortos o regiones muy fuera del dinero.

\subsection{Dominio y muestreo}

Los \emph{datasets} se generan muestreando un hipercubo de inputs. Esta decisión sigue la lógica de Chen et al.: tratamos los parámetros del modelo como pseudo-estados para que una única red aprenda la función de pricing en muchas parametrizaciones posibles. La distribución base es uniforme, porque cubre el dominio completo y no replica únicamente las regiones más frecuentes del mercado. La \autoref{tab:dominio} fija el dominio contractual común y el dominio adicional de Heston.

\begin{table}[ht]
\centering
\small
\begin{tabular}{lll}
\toprule
Variable & Rango & Uso \\
\midrule
$m = S/K$ & $[0.4,\ 2.0]$ & BS y Heston \\
$T$ & $[7/365,\ 2.0]$ & BS y Heston \\
$r$ & $[0.00,\ 0.075]$ & BS y Heston \\
$q$ & $0$ & BS y Heston \\
$\sigma$ & $[0.03,\ 1.00]$ & solo BS \\
\midrule
$v_0$ & $[0.0009,\ 1.00]$ & Heston, $\sqrt{v_0} \in [0.03, 1.00]$ \\
$\theta$ & $[0.0009,\ 1.00]$ & Heston, $\sqrt{\theta} \in [0.03, 1.00]$ \\
$\kappa$ & $[0.10,\ 10.00]$ & Heston, reversión lenta a rápida \\
$\xi$ & $[0.10,\ 3.00]$ & Heston, vol-of-vol moderada a extrema \\
$\rho$ & $[-0.95,\ -0.05]$ & Heston, \emph{skew} negativo típico de \emph{equity} \\
\bottomrule
\end{tabular}
\caption{Dominio contractual común y dominio adicional de Heston. Es deliberadamente amplio: incluye \emph{weeklies}, \emph{wings} profundas y volatilidades de estrés.}
\label{tab:dominio}
\end{table}

La elección de $v_0$ y $\theta$ se expresa en varianza pero se interpreta a través de su raíz cuadrada porque esa es la escala financiera natural. El rango cubre desde volatilidades bajas hasta episodios de estrés severo sin forzar al solver a vivir permanentemente en casos extremos. No imponemos la condición de Feller $2\kappa\theta \geq \xi^2$ como restricción de muestreo. Tratarla como filtro obligatorio haría el problema más limpio pero menos representativo de calibraciones reales, donde muchas violaciones aparecen en condiciones de mercado normales. En su lugar, se guarda como variable diagnóstica.

El muestreo \emph{baseline} es uniforme en la escala financiera elegida. Para Black-Scholes, esto significa uniforme directo sobre $m$, $T$, $r$ y $\sigma$. Para Heston, $\sqrt{v_0}$ y $\sqrt{\theta}$ se muestrean uniformemente en $[0.03, 1.00]$ y después se elevan al cuadrado; $\kappa$, $\xi$ y $\rho$ se muestrean uniformemente en sus rangos. Esta decisión evita una distorsión importante: una uniforme directa sobre varianza sobreponderaría volatilidades muy altas.

La comparación de métodos de muestreo se concentra únicamente en el experimento E3. El resto de experimentos usa el \emph{baseline} uniforme. En E3 se añade un \emph{sampler} enfocado, con más masa cerca del \emph{at-the-money} y de vencimientos cortos, donde la función de pricing tiene mayor curvatura. La construcción concreta es una mezcla 50/50: la mitad de las muestras sigue el \emph{baseline} uniforme completo y la otra mitad usa $m \sim \mathrm{TruncNormal}(1.0, 0.15, [0.7, 1.3])$ y $T \sim \mathrm{LogUniform}(7/365, 0.25)$. La \autoref{fig:samplers} compara ambas distribuciones en el plano $(m, T)$.

% --- FIGURA: sampler uniforme vs focused -------------------------------------
\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.46\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
  width=\textwidth, height=5.6cm,
  xmin=0.4, xmax=2.0, ymin=0, ymax=2.0,
  xlabel={$m = S/K$}, ylabel={$T$ (años)},
  xtick={0.4,0.7,1.0,1.3,1.6,2.0}, ytick={0,0.5,1,1.5,2},
  tick style={color=papergrey}, ticklabel style={font=\scriptsize},
  every axis/.append style={font=\scriptsize, color=papergrey},
  title={Sampler uniforme}, title style={font=\scriptsize\bfseries, color=paperblue}
]
  \addplot[only marks, mark=*, mark size=0.5pt, paperblue, opacity=0.55]
    table[row sep=\\] {
      x y \\ 0.55 1.42 \\ 0.62 0.31 \\ 0.78 1.85 \\ 0.71 0.55 \\ 0.84 1.21 \\
      0.91 0.73 \\ 0.96 1.55 \\ 1.05 0.18 \\ 1.12 0.92 \\ 1.18 1.71 \\
      1.24 0.41 \\ 1.33 1.08 \\ 1.41 1.79 \\ 1.48 0.27 \\ 1.55 0.88 \\
      1.62 1.34 \\ 1.71 0.62 \\ 1.79 1.92 \\ 1.85 0.49 \\ 1.92 1.15 \\
      0.46 0.91 \\ 0.59 0.65 \\ 0.73 1.42 \\ 0.83 0.34 \\ 0.93 1.78 \\
      1.02 0.54 \\ 1.13 1.31 \\ 1.21 0.21 \\ 1.34 0.78 \\ 1.43 1.59 \\
      1.52 0.41 \\ 1.61 1.04 \\ 1.74 1.66 \\ 1.83 0.61 \\ 1.93 1.42 \\
      0.51 1.21 \\ 0.65 1.64 \\ 0.81 0.51 \\ 0.95 0.28 \\ 1.07 1.74 \\
      1.16 0.49 \\ 1.27 1.15 \\ 1.38 1.85 \\ 1.46 0.71 \\ 1.57 1.46 \\
      1.66 0.32 \\ 1.78 1.21 \\ 1.86 1.71 \\ 1.95 0.85 \\ 0.42 0.47 \\
      0.58 1.05 \\ 0.69 1.78 \\ 0.85 0.65 \\ 0.99 1.28 \\ 1.09 0.39 \\
      1.19 1.62 \\ 1.31 0.84 \\ 1.42 0.16 \\ 1.51 1.45 \\ 1.59 1.92 \\
      1.69 0.74 \\ 1.81 1.31 \\ 1.89 0.55 \\ 1.97 1.65 \\ 0.49 0.78 \\
      0.63 1.39 \\ 0.76 0.45 \\ 0.89 1.81 \\ 1.01 0.92 \\ 1.14 1.43 \\
      1.23 0.28 \\ 1.36 1.51 \\ 1.45 0.95 \\ 1.56 0.18 \\ 1.65 1.68 \\
      1.73 0.52 \\ 1.84 1.21 \\ 1.91 0.39 \\ 1.99 1.05 \\
    };
\end{axis}
\end{tikzpicture}
\caption{El muestreo uniforme cubre todo el hipercubo con densidad constante.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.46\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
  width=\textwidth, height=5.6cm,
  xmin=0.4, xmax=2.0, ymin=0, ymax=2.0,
  xlabel={$m = S/K$}, ylabel={$T$ (años)},
  xtick={0.4,0.7,1.0,1.3,1.6,2.0}, ytick={0,0.5,1,1.5,2},
  tick style={color=papergrey}, ticklabel style={font=\scriptsize},
  every axis/.append style={font=\scriptsize, color=papergrey},
  title={Sampler enfocado (mezcla 50/50)}, title style={font=\scriptsize\bfseries, color=paperred}
]
  % background uniform (faded)
  \addplot[only marks, mark=*, mark size=0.5pt, papergrey, opacity=0.25]
    table[row sep=\\] {
      x y \\ 0.55 1.42 \\ 0.78 1.85 \\ 0.62 0.31 \\ 0.84 1.21 \\
      0.91 0.73 \\ 1.12 0.92 \\ 1.18 1.71 \\ 1.33 1.08 \\
      1.48 0.27 \\ 1.55 0.88 \\ 1.71 0.62 \\ 1.85 0.49 \\
      1.92 1.15 \\ 0.59 0.65 \\ 0.73 1.42 \\ 0.93 1.78 \\
      1.13 1.31 \\ 1.34 0.78 \\ 1.52 0.41 \\ 1.74 1.66 \\
      1.93 1.42 \\ 0.65 1.64 \\ 0.95 0.28 \\ 1.27 1.15 \\
      1.46 0.71 \\ 1.66 0.32 \\ 1.86 1.71 \\ 0.69 1.78 \\
      0.85 0.65 \\ 1.09 0.39 \\ 1.31 0.84 \\ 1.51 1.45 \\
      1.69 0.74 \\ 1.97 1.65 \\
    };
  % focused: tight around (1.0, 0.05) and short T
  \addplot[only marks, mark=*, mark size=0.5pt, paperred, opacity=0.75]
    table[row sep=\\] {
      x y \\ 0.92 0.04 \\ 0.97 0.08 \\ 1.02 0.05 \\ 1.06 0.11 \\
      1.10 0.06 \\ 0.93 0.09 \\ 1.00 0.12 \\ 0.95 0.07 \\
      1.03 0.18 \\ 0.99 0.03 \\ 1.07 0.05 \\ 1.01 0.21 \\
      0.94 0.14 \\ 1.05 0.06 \\ 0.91 0.16 \\ 1.08 0.19 \\
      1.04 0.10 \\ 0.96 0.22 \\ 0.98 0.15 \\ 1.09 0.08 \\
      1.11 0.04 \\ 0.89 0.06 \\ 0.93 0.19 \\ 1.02 0.07 \\
      1.06 0.17 \\ 0.97 0.10 \\ 1.00 0.06 \\ 1.04 0.14 \\
      0.95 0.05 \\ 1.07 0.13 \\ 0.99 0.20 \\ 1.03 0.04 \\
      1.05 0.09 \\ 1.01 0.16 \\ 0.92 0.11 \\ 1.08 0.12 \\
      1.10 0.07 \\ 0.94 0.18 \\ 0.96 0.13 \\ 1.02 0.06 \\
      1.01 0.09 \\ 0.99 0.15 \\ 1.05 0.10 \\ 1.07 0.21 \\
    };
  % highlight focus region
  \draw[paperred, thick, dashed] (axis cs:0.7, 0) rectangle (axis cs:1.3, 0.25);
  \node[paperred, font=\scriptsize\itshape] at (axis cs:1.0, 0.45) {zona ATM corta};
\end{axis}
\end{tikzpicture}
\caption{El muestreo enfocado concentra masa en \emph{at-the-money} y vencimientos cortos sin perder cobertura del resto.}
\end{subfigure}
\caption{Comparación del \emph{sampler} uniforme (H-3) y del \emph{sampler} enfocado (H-5). Ambos surrogates se evalúan sobre el mismo \emph{test set} balanceado para que la comparación E3 mida dónde gana y dónde pierde el enfoque local.}
\label{fig:samplers}
\end{figure}

\subsection{Particiones y evaluación por bins}

La partición de datos tiene tres niveles. El entrenamiento usa la distribución propia de cada surrogate. La validación usa una muestra independiente uniforme de cincuenta mil puntos por familia de modelo y sirve para monitorizar convergencia, activar el \emph{scheduler} y seleccionar el \emph{checkpoint}. La regla de selección es siempre el menor $\text{MAE}(C/K)$ en validación, incluso cuando la pérdida de entrenamiento incluye Delta. Mantener la regla fija es importante para que las comparaciones sean limpias: si el surrogate entrenado con DML se seleccionase con una métrica combinada de precio y Delta, no sabríamos si la mejora viene de la pérdida diferencial o de haber escogido otro punto de la trayectoria de entrenamiento.

El test final es independiente y balanceado por \emph{bins}, y se comparte por todos los surrogates de una misma familia. Cada uno de los veinticinco \emph{bins} tiene cinco mil puntos, para un total de ciento veinticinco mil observaciones. Esta decisión es especialmente importante en E3: si evaluásemos H-5 en una muestra con la misma concentración con la que entrena, confundiríamos precisión local con mejora global. El test balanceado permite ver exactamente dónde gana y dónde pierde el \emph{sampler} enfocado.

La evaluación por \emph{bins} es obligatoria porque los errores medios globales son insuficientes. Una red puede tener buen MAE agregado y, aun así, fallar sistemáticamente en opciones ATM de vencimiento corto o en regiones OTM donde la Vega es baja. Por eso usamos una partición $5 \times 5$ por moneyness y vencimiento, representada en la \autoref{fig:bins}. Las métricas se reportan en un dominio interior que recorta un cinco por ciento de cada dimensión del hipercubo, para que los errores de frontera no dominen el promedio global.

% --- FIGURA: bins 5x5 -------------------------------------------------------
\begin{figure}[ht]
\centering
\begin{tikzpicture}[x=1.5cm, y=1.05cm, font=\scriptsize]
  % grid bins 5x5
  \foreach \i [count=\ix from 0] in {Weekly, Short, M-short, Medium, Long} {
    \foreach \j [count=\jy from 0] in {Deep OTM, OTM, ATM, ITM, Deep ITM} {
      \pgfmathtruncatemacro\crit{(\jy == 2 && \ix <= 2) ? 1 : 0}
      \ifnum\crit=1
        \filldraw[fill=uniyellow, draw=paperblue, line width=0.5pt]
          (\jy, \ix) rectangle (\jy+1, \ix+1);
      \else
        \filldraw[fill=uniyellow!18, draw=papergrey!40, line width=0.3pt]
          (\jy, \ix) rectangle (\jy+1, \ix+1);
      \fi
    }
  }
  % labels columns (moneyness)
  \node at (0.5, -0.45) {Deep OTM};
  \node at (1.5, -0.45) {OTM};
  \node at (2.5, -0.45) {ATM};
  \node at (3.5, -0.45) {ITM};
  \node at (4.5, -0.45) {Deep ITM};
  \node[align=center, font=\scriptsize\bfseries, paperblue] at (2.5, -0.95)
    {moneyness $m = S/K$};
  % numeric ranges below
  \node at (0.5, -0.18) {\scriptsize $[0.4,\,0.7)$};
  \node at (1.5, -0.18) {\scriptsize $[0.7,\,0.9)$};
  \node at (2.5, -0.18) {\scriptsize $[0.9,\,1.1]$};
  \node at (3.5, -0.18) {\scriptsize $(1.1,\,1.3]$};
  \node at (4.5, -0.18) {\scriptsize $(1.3,\,2.0]$};

  % labels rows (maturity)
  \foreach \lab [count=\i from 0] in {Weekly, Short, M-short, Medium, Long} {
    \node[anchor=east] at (-0.1, \i+0.5) {\lab};
  }
  \node[rotate=90, font=\scriptsize\bfseries, paperblue] at (-1.45, 2.5) {vencimiento $T$};

  % annotation
  \node[notebox, anchor=west, text width=4.2cm] at (5.4, 2.5)
    {\emph{bins} críticos de E3 \\(ATM \& vencimiento corto, en azul intenso)};
  \draw[paperblue, thick] (5.35, 2.5) -- (4.95, 2.5);
\end{tikzpicture}
\caption{Partición $5 \times 5$ del dominio en \emph{bins} de moneyness y vencimiento. Los \emph{bins} ATM combinados con \emph{weekly}, \emph{short} y \emph{medium-short} (sombreados en azul intenso) son los críticos para E3 por su mayor curvatura.}
\label{fig:bins}
\end{figure}

\subsection{Constantes del pipeline}

Para evitar reabrir discusiones durante la ejecución, fijamos los siguientes valores en código y no los movemos salvo que un hallazgo experimental lo justifique. Cada uno tiene una razón concreta que documentamos a continuación para que el porqué no se pierda con el tiempo.

\begin{table}[ht]
\centering
\small
\begin{tabular}{ll}
\toprule
Parámetro & Valor \\
\midrule
Arquitectura & MLP, 4 capas ocultas, 128 unidades \\
Activación por defecto & Swish \\
Optimizador & Adam, $\text{lr}=10^{-3}$, \emph{scheduler} reduce-on-plateau \\
\emph{Batch size} & 1024 (\emph{baseline}) \\
Épocas máximas & 100 \\
Regularización & ninguna (datos sintéticos) \\
Moneyness & $m = S/K$ \\
Normalización de inputs & min-max a $[0, 1]$ \\
\emph{Output} & precio normalizado $C/K$ \\
\emph{Dividend yield} & $q = 0$ \\
Recorte de bordes & 5\% en cada cara del hipercubo \\
Semilla & fijada por configuración \\
\bottomrule
\end{tabular}
\caption{Constantes del \emph{pipeline} comunes a todos los \emph{surrogates}.}
\label{tab:constantes}
\end{table}

Un MLP \emph{feed-forward} simple es suficiente porque el teorema universal de aproximación garantiza que basta para funciones suaves en dimensión moderada, y Chen et al. confirman empíricamente que funciona en Bates en dimensión trece. Cuatro capas ocultas y 128 unidades por capa dan del orden de cincuenta mil parámetros. Swish entra como activación por defecto porque, según Chen, es la decisión clave para que las Deltas obtenidas por \emph{autograd} sean estables. Adam con $\text{lr}=10^{-3}$ es el \emph{default} de PyTorch y converge sin sintonización en la inmensa mayoría de problemas de regresión, y el \emph{scheduler} \emph{reduce-on-plateau} divide el \emph{learning rate} por dos cuando la pérdida de validación se estanca.

La ausencia de regularización es la decisión metodológica más importante y viene directamente de Chen. En aprendizaje automático clásico la regularización existe porque los datos llevan ruido y la red tendería a memorizarlo. En un surrogate, los \emph{targets} vienen del propio solver, son deterministas y no hay nada que memorizar; la pérdida de validación nunca empeora por \emph{overfitting}, solo se estanca cuando la red llega al límite de su capacidad expresiva. Por eso no usamos \emph{early stopping}, \emph{dropout} ni \emph{weight decay}. El tope de cien épocas funciona como límite práctico.

% =============================================================================
\section{Diseño experimental}\label{sec:experimentos}
% =============================================================================

Cinco experimentos cubren los cinco elementos diferenciales descritos en la \autoref{sec:enfoque}. La elección de exactamente cinco no es arbitraria: es la lectura directa del bloque de elementos diferenciales, traducido a comparaciones experimentales concretas. Cada experimento responde a una pregunta única, varía una sola dimensión y mantiene todo lo demás fijo. Esta rigidez es lo que permite atribuir cualquier diferencia observada a la dimensión que estamos variando y no a otras variables que se nos hayan colado por accidente.

\newcolumntype{R}[1]{>{\raggedright\arraybackslash}p{#1}}
\begin{table}[ht]
\centering
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{R{0.5cm} R{4.0cm} R{2.5cm} R{2.5cm} R{4.2cm}}
\toprule
ID & Pregunta & Lo que varía & Surrogates & Resultado esperado \\
\midrule
\textbf{E1} & ¿Un error bajo en precio implica un error bajo en IV? & Métrica de evaluación & BS-3, H-3 & \emph{Bins} donde el MAE de precio es bajo pero el de IV no lo es. \\
\textbf{E2} & ¿Qué activación produce mejores \emph{Greeks} sin degradar el precio? & Activación & BS-1..4, H-1..4 & MAE de precio similar, MAE de Delta mayor en ReLU. \\
\textbf{E3} & ¿El muestreo enfocado reduce el error en \emph{bins} críticos? & Distribución de muestreo & H-3 vs H-5 & H-5 mejor en ATM y vencimiento corto, posiblemente peor en \emph{wings}. \\
\textbf{E4} & ¿Qué \emph{speedup} ofrece el surrogate vs el solver? & Tamaño de lote & H-3 & \emph{Speedup} creciente con el lote, especialmente en GPU. \\
\textbf{E5} & ¿Entrenar con Delta diferencial mejora la eficiencia muestral? & \emph{Targets} de entrenamiento & H-3-small, H-6-small, H-3 & H-6-small con mejor Delta y precio comparable a H-3-small. \\
\bottomrule
\end{tabular}
\caption{Los cinco experimentos del trabajo. Cada uno responde a una pregunta única, varía una sola dimensión y mantiene todo lo demás fijo.}
\label{tab:experimentos}
\end{table}

E1 es observacional. No requiere entrenar nada nuevo, simplemente recalcula métricas sobre un surrogate que ya está entrenado para otros experimentos. La razón por la que merece llamarse experimento aparte es que la pregunta que responde es metodológica: nos dice si dos surrogates con MAE de precio aparentemente similares pueden tener errores muy distintos en la métrica que de verdad importa en el mercado, que es la volatilidad implícita.

E2 es el experimento más cargado en surrogates porque la activación es la única decisión arquitectónica que afecta directamente la calidad de las derivadas. La intuición se ve en la \autoref{fig:activations}: ReLU, \emph{Softplus}, Swish y \emph{tanh} producen perfiles de salida parecidos, pero solo ReLU tiene una derivada discontinua en cero, mientras que las otras tres son suaves en todo el dominio. Cuando la Delta del surrogate se obtiene por \emph{autograd}, esa discontinuidad de la activación se propaga al gradiente y degrada la calidad de la Delta aprendida, aunque el error en precio sea comparable. Entrenar las cuatro activaciones sobre Black-Scholes nos sirve como control en un entorno donde conocemos la solución exacta y las Deltas analíticas, y entrenarlas sobre Heston confirma o desmiente el patrón en el caso real.

% --- FIGURA: activaciones y sus derivadas -----------------------------------
\begin{figure}[ht]
\centering
\begin{subfigure}[t]{0.49\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
  width=\textwidth, height=5.5cm,
  axis lines=middle, axis line style={-{Latex[length=2mm]}, papergrey},
  xmin=-3, xmax=3, ymin=-1.3, ymax=3,
  xtick={-3,-2,-1,0,1,2,3}, ytick={-1,0,1,2,3},
  tick style={color=papergrey}, ticklabel style={font=\scriptsize},
  xlabel={$x$}, ylabel={$\sigma(x)$},
  every axis/.append style={font=\scriptsize, color=papergrey},
  legend style={draw=papergrey!30, fill=white, font=\scriptsize, at={(0.02,0.98)}, anchor=north west, row sep=-2pt}
]
  \addplot[domain=-3:3, samples=120, paperred, thick] {max(0,x)};
  \addlegendentry{ReLU}
  \addplot[domain=-3:3, samples=120, paperorange, thick] {ln(1+exp(x))};
  \addlegendentry{\emph{Softplus}}
  \addplot[domain=-3:3, samples=120, papergreen, thick] {x/(1+exp(-x))};
  \addlegendentry{Swish/SiLU}
  \addplot[domain=-3:3, samples=120, paperpurple, thick] {tanh(x)};
  \addlegendentry{tanh}
\end{axis}
\end{tikzpicture}
\caption{Funciones de activación.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.49\textwidth}
\centering
\begin{tikzpicture}
\begin{axis}[
  width=\textwidth, height=5.5cm,
  axis lines=middle, axis line style={-{Latex[length=2mm]}, papergrey},
  xmin=-3, xmax=3, ymin=-0.3, ymax=1.4,
  xtick={-3,-2,-1,0,1,2,3}, ytick={0,0.5,1},
  tick style={color=papergrey}, ticklabel style={font=\scriptsize},
  xlabel={$x$}, ylabel={$\sigma'(x)$},
  every axis/.append style={font=\scriptsize, color=papergrey},
  legend style={draw=papergrey!30, fill=white, font=\scriptsize, at={(0.98,0.02)}, anchor=south east, row sep=-2pt}
]
  \addplot[domain=-3:-0.001, samples=2, paperred, thick] {0};
  \addplot[domain=0.001:3, samples=2, paperred, thick] {1};
  \addplot[only marks, mark=o, mark size=1.4pt, paperred] coordinates {(0,0) (0,1)};
  \addlegendentry{ReLU'}
  \addplot[domain=-3:3, samples=120, paperorange, thick] {1/(1+exp(-x))};
  \addlegendentry{\emph{Softplus}'}
  \addplot[domain=-3:3, samples=120, papergreen, thick]
    {1/(1+exp(-x)) + x*exp(-x)/((1+exp(-x))^2)};
  \addlegendentry{Swish'}
  \addplot[domain=-3:3, samples=120, paperpurple, thick] {1 - tanh(x)*tanh(x)};
  \addlegendentry{tanh'}
\end{axis}
\end{tikzpicture}
\caption{Derivadas.}
\end{subfigure}
\caption{Las cuatro activaciones del experimento E2 y sus derivadas. La derivada de ReLU es discontinua en cero, lo que se propaga a las Deltas calculadas por \emph{autograd}; las otras tres son suaves en todo el dominio.}
\label{fig:activations}
\end{figure}

E3 ataca una idea clásica de métodos numéricos transferida al dominio de los \emph{surrogates}: concentrar el esfuerzo computacional en las regiones donde la función es más difícil de aproximar. La comparación es limpia porque H-3 y H-5 difieren solo en la distribución de muestreo: misma arquitectura, mismo número total de puntos, misma pérdida y mismo tamaño total del \emph{dataset}.

E4 es de medición, no de comparación. Cronometra evaluaciones de H-3 contra el solver original de Heston en lotes de tamaño creciente. Reportar el \emph{speedup} como función del tamaño de lote es más honesto que dar un único número que dependería arbitrariamente del lote elegido.

E5 prueba la hipótesis central del paper de Huge y Savine en nuestro \emph{setting} de forma acotada: si además de precios generamos Delta verdadera, ¿puede el surrogate aprender mejor la forma local de la función de pricing con menos datos? Delta es la \emph{Greek} más limpia para este primer experimento porque, usando $y = C/K$ y $m = S/K$, se cumple $\Delta = \partial y / \partial m$.

Cada experimento tiene una métrica primaria alineada con su pregunta y métricas secundarias como control. Esto evita que el resultado se reduzca a una tabla grande sin conclusión clara.

\begin{table}[ht]
\centering
\small
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{R{0.5cm} R{5.2cm} R{7.5cm}}
\toprule
ID & Métrica primaria & Controles y diagnóstico \\
\midrule
E1 & Discrepancia $\text{MAE}(C/K)$ vs $\text{MAE}_{\text{IV}}$ por \emph{bin} & Sin umbral; Vega/proxy, percentiles altos y fallos de IV \\
E2 & $\text{MAE}_\Delta$ por \emph{bin} & Sin umbral; activaciones suaves vs ReLU y precio como control \\
E3 & $\text{MAE}_{\text{IV}}$ en \emph{bins} ATM con weekly, short, medium-short & Positivo fuerte si mejora $\geq 10\%$ y global empeora $\leq 10\%$; precio como control \\
E4 & $\text{speedup} = t_{\text{solver}} / t_{\text{surrogate}}$ por tamaño de lote & Lotes $10^2$--$10^5$, 3 \emph{warmups} + 10 repeticiones, mediana, CPU/GPU separado \\
E5 & Mejora de $\text{MAE}_\Delta$ de H-6-small vs H-3-small & Positivo fuerte si $\Delta$ mejora $\geq 20\%$ y precio empeora $\leq 10\%$; distancia frente a H-3 \\
\bottomrule
\end{tabular}
\caption{Métrica primaria y controles para cada experimento.}
\label{tab:metricas}
\end{table}

\subsection{Once surrogates}

Once \emph{surrogates} en total. La cifra sale de trabajar hacia atrás desde los cinco experimentos: cada uno necesita un conjunto mínimo de \emph{surrogates} entrenados para sostener su comparación y muchos se solapan. H-3, el \emph{surrogate} de Heston con configuración \emph{baseline}, aparece como referencia en los cinco experimentos. Esa centralidad evita que el coste computacional total se dispare y justifica que dediquemos especial cuidado a su entrenamiento.

\begin{table}[ht]
\centering
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{lllllc}
\toprule
ID & Modelo & Activación & Pérdida & Dataset & Experimentos \\
\midrule
BS-1 & Black-Scholes & ReLU      & $L_1(\text{precio})$ & 200k uniforme & E2 \\
BS-2 & Black-Scholes & Softplus  & $L_1(\text{precio})$ & 200k uniforme & E2 \\
\textbf{BS-3} & \textbf{Black-Scholes} & \textbf{Swish} & $\boldsymbol{L_1(\text{precio})}$ & \textbf{200k uniforme} & \textbf{E1, E2} \\
BS-4 & Black-Scholes & tanh      & $L_1(\text{precio})$ & 200k uniforme & E2 \\
H-1  & Heston        & ReLU      & $L_1(\text{precio})$ & 500k uniforme & E2 \\
H-2  & Heston        & Softplus  & $L_1(\text{precio})$ & 500k uniforme & E2 \\
\textbf{H-3} & \textbf{Heston} & \textbf{Swish} & $\boldsymbol{L_1(\text{precio})}$ & \textbf{500k uniforme} & \textbf{E1--E5} \\
H-4  & Heston        & tanh      & $L_1(\text{precio})$ & 500k uniforme & E2 \\
H-5  & Heston        & Swish     & $L_1(\text{precio})$ & 500k \textbf{enfocado} & E3 \\
H-3-small & Heston   & Swish     & $L_1(\text{precio})$ & 100k uniforme & E5 \\
H-6-small & Heston   & Swish     & $L_1(\text{precio}) + L_1(\Delta)$ & 100k uniforme + $\Delta$ & E5 \\
\bottomrule
\end{tabular}
\caption{Los once \emph{surrogates} del trabajo. Todos los comparados dentro de un mismo experimento comparten arquitectura, optimizador, hiperparámetros y semilla.}
\label{tab:surrogates}
\end{table}

En E5 sí varía deliberadamente el tamaño del \emph{dataset} porque la pregunta no es solo si DML mejora las \emph{Greeks}, sino si permite alcanzar precisión comparable con menos muestras. Si dejásemos variar la arquitectura entre \emph{surrogates}, la comparación de activaciones quedaría contaminada con el efecto del cambio arquitectónico.

\subsection{Criterios de validez}

Cada experimento se cierra con una comparación que cambia una sola dimensión relevante. Si se cambia activación, no se cambia \emph{dataset}. Si se cambia \emph{sampler}, no se cambia arquitectura. Si se cambia \emph{loss} en E5, se mantiene el mismo tamaño pequeño de \emph{dataset} para H-3-small y H-6-small. Para E3 y E5, en los que sí buscamos una clasificación operativa del resultado, los umbrales \emph{positivo fuerte/débil/negativo} se fijan antes de ver los datos. Esa regla previa es lo que evita interpretar resultados de forma oportunista cuando llegue el momento de redactar conclusiones.

% =============================================================================
\section{Plan de ejecución por fases}\label{sec:fases}
% =============================================================================

El proyecto se divide en cuatro fases ordenadas. Cada una no empieza hasta que la anterior está cerrada y verificada contra un criterio de salida concreto, como ilustra la \autoref{fig:phases}. Esta secuencialidad es estricta porque las fases tienen dependencias reales: no podemos entrenar redes sobre datos que no hemos generado, no podemos generar datos sin \emph{solvers} que funcionen y no podemos validar un experimento sin métricas implementadas.

% --- FIGURA: timeline de fases ----------------------------------------------
\begin{figure}[ht]
\centering
\begin{tikzpicture}[node distance=2.5mm, font=\scriptsize]
  \node[phasebox] (p0) {Fase 0\\\emph{Infraestructura}};
  \node[phasebox, right=of p0] (p1) {Fase 1\\\emph{Pipeline}};
  \node[phasebox, right=of p1] (p2) {Fase 2\\\emph{Experimentos BS}};
  \node[phasebox, right=of p2] (p3) {Fase 3\\\emph{Experimentos Heston}};
  \node[phasebox, right=of p3] (p4) {Fase 4\\\emph{Eficiencia y cierre}};

  \draw[pipearrow] (p0) -- (p1);
  \draw[pipearrow] (p1) -- (p2);
  \draw[pipearrow] (p2) -- (p3);
  \draw[pipearrow] (p3) -- (p4);

  % salidas
  \node[notebox, below=4mm of p0, text width=2.4cm]
    {Error $<10^{-6}$ precio\\ y $<10^{-4}$ Delta vs QuantLib};
  \node[notebox, below=4mm of p1, text width=2.4cm]
    {BS-3 converge a $\text{MAE}<10^{-4}$};
  \node[notebox, below=4mm of p2, text width=2.4cm]
    {E1 + E2 cerrado sobre BS};
  \node[notebox, below=4mm of p3, text width=2.4cm]
    {E2 + E3 + E5 cerrado sobre Heston};
  \node[notebox, below=4mm of p4, text width=2.4cm]
    {\emph{Speedup} para lotes $10^2$--$10^5$};

  % phase status markers
  \node[font=\Large, papergreen, anchor=center] at ([yshift=-0.45cm]p0.north) {\checkmark};
  \node[font=\Large, paperorange, anchor=center] at ([yshift=-0.45cm]p1.north) {$\bullet$};
  \node[font=\Large, papergrey!50, anchor=center] at ([yshift=-0.45cm]p2.north) {$\circ$};
  \node[font=\Large, papergrey!50, anchor=center] at ([yshift=-0.45cm]p3.north) {$\circ$};
  \node[font=\Large, papergrey!50, anchor=center] at ([yshift=-0.45cm]p4.north) {$\circ$};

  % legend
  \node[legendbox, anchor=north west] at ([yshift=-1.8cm]p0.south west) {
    \textcolor{papergreen}{\checkmark}\ cerrada \quad
    \textcolor{paperorange}{$\bullet$}\ en curso \quad
    \textcolor{papergrey!50}{$\circ$}\ pendiente
  };
\end{tikzpicture}
\caption{Plan de ejecución secuencial por fases con su criterio de salida. La secuencialidad es estricta: cada fase depende del cierre verificado de la anterior.}
\label{fig:phases}
\end{figure}

La \textbf{fase 0} cubre la infraestructura. Incluye la estructura del repositorio, semillas centralizadas, \emph{solvers} de Black-Scholes y Heston, inversor de volatilidad implícita y la validación cruzada con QuantLib. Su criterio de salida es que Black-Scholes y Heston reproduzcan valores de referencia externos con error inferior a $10^{-6}$ en precio y $10^{-4}$ en Delta. Esta fase se cierra contra valores tabulados conocidos porque un fallo silencioso en el \emph{solver} contaminaría todo el resto del proyecto sin que nos enteremos.

La \textbf{fase 1} monta el \emph{pipeline} de datos y entrenamiento. Implementa el \emph{sampler}, el generador, el \emph{script} reproducible de generación, el modelo, las utilidades de \emph{Greeks}, el \emph{trainer} y el \emph{script} de entrenamiento. Genera los \emph{datasets} base de entrenamiento, validación y test balanceado por \emph{bins} para ambas familias de modelo. Su criterio de salida es un \emph{smoke test} sobre BS-3 que converja a un MAE de precio razonablemente pequeño en validación. Es la primera vez que el \emph{pipeline} completo se ejecuta de principio a fin: si BS-3 no converge cuando le pasamos datos de una fórmula cerrada conocida, hay algo roto antes de tocar Heston y este es el momento de descubrirlo.

La \textbf{fase 2} ejecuta los experimentos sobre Black-Scholes. Entrena BS-1, BS-2, BS-3 y BS-4, implementa los módulos de \emph{binning} y métricas, produce tablas y \emph{heatmaps} por \emph{bin} para cada \emph{surrogate} y completa los análisis E1 (precio vs IV) y E2 (activaciones) sobre Black-Scholes. El motivo de hacer Black-Scholes antes que Heston es que Black-Scholes tiene fórmulas cerradas para precio, Delta, Vega e IV, lo que permite validar todas las piezas del \emph{pipeline} contra valores exactos antes de extender el mismo análisis a Heston.

La \textbf{fase 3} ejecuta los experimentos sobre Heston, que es el bloque más grande del proyecto. Genera los \emph{datasets} específicos de E3 y E5, entrena H-1, H-2, H-3, H-4, H-5, H-3-small y H-6-small, repite el análisis E2 sobre Heston y cierra E3 (muestreo) y E5 (DML) con su clasificación operativa fuerte/débil/negativa.

La \textbf{fase 4} cubre eficiencia y cierre. Implementa el protocolo de cronometraje con \emph{warmup} y diez repeticiones por lote, ejecuta E4 sobre H-3, produce las figuras finales y redacta la memoria. La eficiencia se mide al final porque depende de tener el \emph{surrogate} ya entrenado y validado.

% =============================================================================
\section{Discusión y trabajo futuro}\label{sec:discusion}
% =============================================================================

El alcance del proyecto está deliberadamente acotado. No intentamos calibrar a datos reales de mercado, ni estudiar opciones exóticas, ni construir un \emph{surrogate} industrial a escala de Chen et al. El objetivo es demostrar, en un entorno controlado y reproducible, cuándo una red puede aproximar un \emph{solver} de pricing con precisión, rapidez y derivadas útiles. La contribución está en la comparación ordenada de métricas, activaciones, muestreo, eficiencia y aprendizaje diferencial, no en maximizar complejidad del modelo a cualquier coste.

Quedan abiertas varias extensiones naturales que merecen mención. Extender el conjunto de \emph{Greeks} incorporadas a la pérdida diferencial (Vega, Gamma, Theta, Rho) permitiría medir hasta qué punto el \emph{differential machine learning} escala más allá del caso unidimensional que estudiamos en E5, aunque introduce dificultades numéricas adicionales porque algunas de esas derivadas son sensibles a la inestabilidad del \emph{solver} de Heston en zonas extremas. Pasar de opciones europeas a opciones americanas o exóticas pondría a prueba la generalización del enfoque a \emph{payoffs} con derecho de ejercicio anticipado y conectaría nuestro trabajo con la línea de Becker, Cheridito y Jentzen. Aplicar el \emph{surrogate} dentro de un procedimiento de calibración a datos reales --- en línea con el segundo nivel de validación de Chen et al.--- mediría el coste económico del error de aproximación de la red. Por último, escalar la arquitectura o el tamaño del \emph{dataset} permitiría explorar si la reducción de error es lineal o se satura, una pregunta especialmente relevante en la fase actual del proyecto.

A día de hoy, las fases 0 y 1 están prácticamente cerradas: los \emph{solvers} están validados contra QuantLib y el \emph{pipeline} de generación y entrenamiento está operativo. Las fases 2 a 4 se ejecutarán en las semanas siguientes según el plan descrito en la \autoref{sec:fases}. La estructura del trabajo está diseñada para que los resultados experimentales se puedan integrar de forma incremental en la versión final del paper sin reescribir las secciones metodológicas, que ya están cerradas.

% =============================================================================
\begin{thebibliography}{99}
% =============================================================================

\bibitem{hutchinson1994}
J.~M. Hutchinson, A.~W. Lo y T. Poggio.
\newblock A nonparametric approach to pricing and hedging derivative securities via learning networks.
\newblock \emph{Journal of Finance}, 49(3):851--889, 1994.

\bibitem{becker2019}
S. Becker, P. Cheridito y A. Jentzen.
\newblock Deep optimal stopping.
\newblock \emph{Journal of Machine Learning Research}, 20:1--25, 2019.

\bibitem{ruf2020}
J. Ruf y W. Wang.
\newblock Neural networks for option pricing and hedging: a literature review.
\newblock \emph{Journal of Computational Finance}, 24(1):1--46, 2020.

\bibitem{chen2025}
H. Chen, A. Didisheim y S. Scheidegger.
\newblock Deep surrogates for finance: with an application to option pricing.
\newblock \emph{Manuscript / SSRN preprint}, 2025.

\bibitem{huge2020}
B. Huge y A. Savine.
\newblock Differential machine learning.
\newblock \emph{Risk Magazine} y SSRN, 2020.

\bibitem{sirignano2018}
J. Sirignano y K. Spiliopoulos.
\newblock DGM: a deep learning algorithm for solving partial differential equations.
\newblock \emph{Journal of Computational Physics}, 375:1339--1364, 2018.

\bibitem{han2018}
J. Han, A. Jentzen y W.~E.
\newblock Solving high-dimensional partial differential equations using deep learning.
\newblock \emph{Proceedings of the National Academy of Sciences}, 115(34):8505--8510, 2018.

\bibitem{ferguson2018}
R. Ferguson y A. Green.
\newblock Deeply learning derivatives.
\newblock \emph{arXiv preprint arXiv:1809.02233}, 2018.

\bibitem{buehler2019}
H. Buehler, L. Gonon, J. Teichmann y B. Wood.
\newblock Deep hedging.
\newblock \emph{Quantitative Finance}, 19(8):1271--1291, 2019.

\bibitem{dlcalib2023}
\emph{Calibrating stochastic volatility models with differential machine learning.}
\newblock \emph{SSRN preprint}, 2023.

\bibitem{barron1993}
A.~R. Barron.
\newblock Universal approximation bounds for superpositions of a sigmoidal function.
\newblock \emph{IEEE Transactions on Information Theory}, 39(3):930--945, 1993.

\bibitem{grohs2018}
P. Grohs, F. Hornung, A. Jentzen y P. von Wurstemberger.
\newblock A proof that artificial neural networks overcome the curse of dimensionality in the numerical approximation of Black-Scholes partial differential equations.
\newblock \emph{arXiv preprint arXiv:1809.02362}, 2018.

\end{thebibliography}

\end{document}
